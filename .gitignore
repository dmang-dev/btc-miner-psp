# pspdev build artifacts (the EBOOT.PBP at repo root IS committed
# so users can grab it without the toolchain — see README "Try it")
build/
*.elf
*.elf.stripped
*.elf.sym
*.map
*.bin
*.PRX
*.prx

# pspdev intermediate metadata (regenerated by build)
PARAM.SFO

# editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
